apiVersion: v1
kind: Service
metadata:
  name: {{ include "imove-backend.fullname" . }}
  labels:
    {{- include "imove-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  loadBalancerIP: {{ .Values.service.loadBalancerIP }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "imove-backend.selectorLabels" . | nindent 4 }}
