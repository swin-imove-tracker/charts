apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "imove-backend.fullname" . }}-test-connection"
  labels:
    {{- include "imove-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "imove-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
